# Initial Setup

```bash
$ sudo gem install backup
$ sudo gem install fog -v '~> 1.1.0'
$ sudo gem install mail -v '~> 2.4.0'
$ sudo gem install whenever
$ git clone git://github.com/lardawge/bunkerfm-backup-config.git Backup
$ cd Backup
```